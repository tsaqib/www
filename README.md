If there is a problem with the `jekyll` installation itself:

```bash
gem sources --remove https://rubygems.org/
gem sources -a http://rubygems.org/
gem install jekyll
```

If gems are not found: 

```bash
gem install jekyll-sitemap
gem install jekyll-seo-tag
```
